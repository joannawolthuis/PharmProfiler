{% if json %}

	{% for obj in json %}

		{% if "ClinicalAnnotation" in obj.connectionTypes %}
		
			INSERT INTO DrugVars VALUES(
				"{{did}}",
				"{{obj.connectedOject.id}}",
				"{{obj.connectedOject.name}}"
			);
		
		{% endif %}

	{% endfor %}

{% endif %}